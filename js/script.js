// Animação de elementos com o Scroll

window.sr = ScrollReveal({ reset: true });

sr.reveal('#principal', { duration: 1000 });

sr.reveal('#trabalhos', { duration: 900 });

sr.reveal('#educacao', { duration: 800 });

sr.reset('#projetos');